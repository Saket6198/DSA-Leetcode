#include<iostream>
#include<
using namespace std;

class Solution{
public:
    int SecondLargest(int arr[], int n){
        for(int i = 0; i<sizeof(arr) / sizeof(int); i++)
    }
};

int main(){
    Solution solution;
    int arr[100], x, size;
    cout<<"Enter the size of array : ";
    cin>>size;
    cout<<"\nEnter the elements of the array : \n";
    for(int i=0; i<size; i++)
        cin>>arr[i];
    cout<<solution.SecondLargest(arr, );
}